. env.sh

mkdir -pv $LFS/{etc,var} $LFS/usr/{bin,lib,sbin}

mkdir -pv $LFS/boot/efi

for i in bin lib sbin; do
  ln -sv usr/$i $LFS/$i
done

case $(uname -m) in
  x86_64) ln -sv usr/lib $LFS/lib64 ;;
esac


mkdir -pv $LFS/tools
